#include <stdio.h>
#include <stdlib.h>
#include "../include/function.h"

int main()
{
    system("cls");      // Xóa
    system("color 0D"); // Màu nền đen, chữ tím
    // adminLogin();      // Đăng nhập với tài khoản Admin
    mainMenu(); // Hiển thị menu chính
    return 0;   // Kết thúc chương trình
}
